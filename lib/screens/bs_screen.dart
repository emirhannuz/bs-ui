import 'package:flutter/material.dart';

class DbsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          "Bilgi Sistemi",
          style: TextStyle(fontSize: 24, color: Colors.grey),
        ),
      ),
    );
  }
}
