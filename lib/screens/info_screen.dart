import 'package:flutter/material.dart';

class YoksisInformationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          "Bilgiler",
          style: TextStyle(fontSize: 24, color: Colors.grey),
        ),
      ),
    );
  }
}
